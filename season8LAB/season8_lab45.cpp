#include <stdio.h>
#include <math.h>
#include "func.h"
int main (){
		int a;
	printf ("nhap so a:");
	scanf ("%d",&a);
	int b;
	printf ("nhap so b:");
	scanf ("%d",&b);
	int x=timUcln (a,b);
	int y=timBcnn(a,b);
}
